#!/usr/bin/env bash
# Thin CLI wrapper — always run as a script (never source this file).
set -euo pipefail
_ROOT="$(cd "$(dirname "$0")" && pwd)"
# shellcheck source=secrets-gpg-provider.sh
. "${_ROOT}/secrets-gpg-provider.sh"
secrets_gpg_cli "$@"
